# Shared contracts

These JSON files are illustrative request and response shapes shared by the Express API and FastAPI service. They are not executable mocks or stored business records. Replace examples with validated schemas as endpoints are implemented.
