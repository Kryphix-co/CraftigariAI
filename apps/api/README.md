# Craftigari API

Express owns Craftigari business data and integrations with MongoDB, Cloudinary, Razorpay, and the internal AI service.

## Development

Copy `.env.example` to `.env`, install dependencies with `npm install`, and start with `npm run dev`.

Only the health endpoint is currently implemented. All business modules are placeholders.
